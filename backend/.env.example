# Example env for local development
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=appdb
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/appdb?schema=app

# S3 / object storage (optional)
S3_ENDPOINT=
S3_BUCKET=
S3_REGION=
S3_ACCESS_KEY=
S3_SECRET_KEY=

# Other secrets
JWT_SECRET=
SESSION_SECRET=

# Do NOT commit real secrets to git. Use a secrets manager for production.
